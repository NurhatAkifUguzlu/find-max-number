# Find Maximum Number

A simple C program that demonstrates the use of arrays and functions.

## Features
- Takes user input for an array.
- Uses a separate function to process the array.
- Finds and prints the maximum value.
